import React from "react";
import { makeStyles } from '@material-ui/core/styles';

const Care = props => {
    return(
        <div>this is customer care
            <h1>HIII</h1>
        </div>

    );
};

export default Care
